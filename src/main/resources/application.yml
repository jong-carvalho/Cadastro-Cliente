
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cadastro_cliente
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  jackson:
    property-naming-strategy: SNAKE_CASE

server:
  port: 8080


management:
  endpoints:
    web:
      exposure:
        include:
          - prometheus
